make any changes needed to the ./src index.js file and save.

To rebuild the package, bring the node_modules, Package.json, Package-lock.json, and
webpack.config.json files from the archive and place in the EmojiPickcer folder

right click to open a terminal/Console window in the Emoji picker folder

enter: npm run build

this will rebuild entire package with your changes